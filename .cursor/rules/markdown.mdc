---
description: Markdown style
globs: "**/*.md"
alwaysApply: false
---

# Markdown

- Headings: first letter of first word uppercase, other words not automatically capitalized.
- Avoid `→`. Instead, use `>` in describing a sequence of options chosen from system menus, and -> elsewhere.
